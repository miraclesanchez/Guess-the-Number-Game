package Abstraction;
//interface created because we know an eagle is a bird but also an aminal so this will allow the eagle to use information from both interfaces

public interface animal_interface {
	public void eat();

}
